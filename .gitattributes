# 统一行尾：仓库里一律 LF，检出到工作区也一律 LF。
#
# 背景：本仓库所有 blob 本来就是 LF，但开发机上 core.autocrlf=true 会在
# 检出时把文本文件转成 CRLF。server-go/internal/httpapi/adminhtml_test.go
# 里有若干断言用 "...\n..." 字面量去匹配 web/admin.html 的源码，CRLF 下
# 永远匹配不上，导致守卫测试在干净检出时长期飘红。
# eol 属性优先级高于 core.autocrlf，所以下面的声明能让检出结果与各人的
# 全局配置无关。

# 兜底：自动识别文本文件，检出为 LF；二进制文件自动放行不做转换。
* text=auto eol=lf

# ---- 显式声明文本类型（可读性 + 防止自动识别走眼）----
*.go        text eol=lf
*.html      text eol=lf
*.htm       text eol=lf
*.js        text eol=lf
*.mjs       text eol=lf
*.cjs       text eol=lf
*.ts        text eol=lf
*.css       text eol=lf
*.md        text eol=lf
*.txt       text eol=lf
*.sql       text eol=lf
*.json      text eol=lf
*.yaml      text eol=lf
*.yml       text eol=lf
*.xml       text eol=lf
*.java      text eol=lf
*.py        text eol=lf
*.gradle    text eol=lf
*.properties text eol=lf
*.pro       text eol=lf
*.caddy     text eol=lf
*.service   text eol=lf
*.example   text eol=lf
*.mod       text eol=lf
*.sum       text eol=lf
.gitignore  text eol=lf
.gitattributes text eol=lf
.dockerignore text eol=lf
Dockerfile* text eol=lf

# Shell 脚本必须是 LF，否则在 Linux 上 `#!/bin/sh\r` 直接执行失败。
*.sh        text eol=lf
gradlew     text eol=lf

# Windows 批处理保持 CRLF（cmd.exe 对 LF 的 .bat 行为不稳）。
*.bat       text eol=crlf
*.cmd       text eol=crlf

# ---- 二进制：显式关掉一切行尾/文本处理，别被规范化 ----
*.png       -text
*.jpg       -text
*.jpeg      -text
*.gif       -text
*.webp      -text
*.bmp       -text
*.ico       -text
*.svgz      -text
*.pdf       -text
*.mp4       -text
*.mov       -text
*.mp3       -text
*.wav       -text
*.otf       -text
*.ttf       -text
*.woff      -text
*.woff2     -text
*.eot       -text
*.jar       -text
*.war       -text
*.zip       -text
*.gz        -text
*.tgz       -text
*.bz2       -text
*.xz        -text
*.7z        -text
*.apk       -text
*.aab       -text
*.aar       -text
*.dex       -text
*.so        -text
*.dll       -text
*.dylib     -text
*.exe       -text
*.keystore  -text
*.jks       -text
*.p12       -text
*.pfx       -text
*.db        -text
*.sqlite    -text
*.sqlite3   -text
