# MuseFrame 后台安全复审（2026-09-07）

## 结论

本轮确认并修复 4 组可操作问题：一项未认证拒绝服务、一项购买凭证跨账号领取风险、一项长期会话令牌 URL 暴露，以及一项生产依赖高危公告。并补齐请求字段边界、安全响应头、管理员令牌存储、容器与 Caddy 纵深防御。

“通过本轮审计”不等于不存在任何未知漏洞；下面列出仍需持续处理的兼容性和运维风险。

## 已修复

| 等级 | 问题 | 修复 |
|---|---|---|
| 严重 | 畸形 `Host` 在请求异常边界外触发 `ERR_INVALID_URL`，单个匿名请求可终止 Node 进程 | 不再以 Host 构造路由 URL；非法 Host 返回 400，并验证进程继续健康 |
| 高 | `(platform, transaction)` 虽全局唯一，但已有购买记录未校验 `user_id/product_id` | pending、Google 订单号规范化及最终授予前均执行永久账号/商品绑定校验，冲突返回 409 |
| 高 | 当前网页把 90 天会话 Bearer 放在图片 `?token=` 中 | 网页改为预取并缓存一小时账号范围 `img_token`；新 URL 不再包含会话 Bearer |
| 高 | `nodemailer` 6.x 命中生产依赖高危公告 | 升级到 10.0.1；生产依赖审计为 0 |
| 中 | 邮箱、上传、项目、生成、购买及幂等键存在类型/长度空洞 | 增加统一字符串、整数、枚举、所有权和长度校验 |
| 中 | 应用自身缺少 CSP、点击劫持、Referrer Policy、nosniff 等基线头 | 在 Node 边界统一发送安全头，API 响应 `no-store` |
| 中 | 管理员令牌持久化在 `localStorage` | 改为 tab 级 `sessionStorage` 并清除旧持久副本 |
| 中 | 容器根文件系统可写且保留 capabilities | Compose 启用只读根、`cap_drop: ALL`、`no-new-privileges`、PID 上限和受限 tmpfs |
| 高 | 仓库 Caddy 模板保留 nip.io 明文入口 | 删除明文入口、正式 HTTP 强制跳 HTTPS、剥离可伪造的 `CF-Connecting-IP` |

此外，已完整排空的 413 请求不再强制关闭健康连接，消除了客户端连接池在大包拒绝后的稳定失败。

## 验证

- JavaScript 语法检查通过。
- HTTP 安全回归连续两轮 `37/37` 通过。
- 全套自动化测试 `154/154` 通过。
- `npm audit --omit=dev`：0 vulnerabilities。
- `git diff --check`：通过。
- Compose 语法由生产主机实际 Docker Compose 在部署阶段校验（本地 Windows 未安装 Docker CLI）。

## 剩余风险 / 后续项

- 为兼容已安装的旧客户端，服务端资产接口仍暂时接受“仅限该路由”的会话查询参数；当前网页和新版客户端已迁移到短期图片令牌。应在旧客户端覆盖率允许后删除兼容入口。
- 完整 npm 审计仍报告 Capacitor CLI 开发链 4 个中危告警；它们不进入 `npm ci --omit=dev` 的生产镜像，当前 registry 修复建议是降级 CLI，需单独验证 Android 构建兼容性。
- 同步 JPEG 解码仍可能造成事件循环短时阻塞（已有 40MP 上限）；后续可迁移到 worker thread。
- 管理员 SQL 控制台仍缺数据库级硬超时；目前仅管理员令牌可访问。
- Google Play 退款/撤销的周期巡检仍需接入实时开发者通知或定时核验。
